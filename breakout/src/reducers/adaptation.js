const initialState = {
  dimension: "speed"
};

export const adaptation = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};

export default adaptation;
