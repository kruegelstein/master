// @flow

// Constants
import {} from "../constants/ActionTypes.js";

const initialState = {};

export const game = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};

export default game;
